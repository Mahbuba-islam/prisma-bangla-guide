# 📘 **Prisma ORM — নতুনদের জন্য সহজ বাংলায় গাইড**

## 🌟 Prisma ORM কী?

**Prisma** হলো একটা আধুনিক টুল যা Node.js আর TypeScript-এর সাথে ব্যবহার হয়। এটা একটা **ORM** (Object-Relational Mapping) মানে ডাটাবেজের সাথে কাজ করার সহজ উপায়।

👉 সাধারণত ডাটাবেজে কাজ করতে raw SQL লিখতে হয়, কিন্তু Prisma দিয়ে তা ছাড়াই সহজ কোড লিখে কাজ করা যায়। আর কোডটা **type-safe** — মানে TypeScript-এ ভুল হলে আগেই জানিয়ে দেয়।

### Prisma-এর ভালো দিকগুলো :

- ডাটা মডেল (টেবিল) বানানো খুব সহজ
- কোড লিখতে গেলে VS Code-এ অটো-কমপ্লিট দেখায় (ভুল কম হয়)
- ডাটাবেজ পরিবর্তন অটোমেটিক ম্যানেজ করে (migration)
- অনেক ডাটাবেজ সাপোর্ট করে
- 

> Prisma তোমার ডাটাবেজকে একটা সুন্দর, নিরাপদ API-তে পরিণত করে।

---

## 🗄️ Prisma কোন কোন ডাটাবেজ সাপোর্ট করে?

Prisma অনেক জনপ্রিয় ডাটাবেজের সাথে কাজ করে:

- PostgreSQL
- MySQL (আর MariaDB)
- SQLite
- SQL Server
- MongoDB
- CockroachDB

👉 একই কোড দিয়ে বিভিন্ন ডাটাবেজ চেঞ্জ করা যায়!

---

## 🔧 Prisma-এর প্রধান তিনটা অংশ

### 1️⃣ **Prisma Client**
   - তোমার কোড থেকে ডাটাবেজে কোয়েরি চালানোর জন্য অটো-জেনারেটেড টুল
   - পুরোপুরি type-safe + VS Code-এ সাজেশন দেয়

### 2️⃣ **Prisma Migrate**
   - ডাটাবেজের স্ট্রাকচার (schema) পরিবর্তন করার জন্য
   - অটোমেটিক migration ফাইল বানায় ও ডাটাবেজে অ্যাপ্লাই করে

### 3️⃣ **Prisma Studio**
   - ডাটাবেজের ডাটা দেখার ও এডিট করার সহজ GUI টুল (অ্যাডমিন প্যানেলের মতো)

---


## 🧠 এক লাইনে Prisma

**Prisma ডাটাবেজের কাজকে সহজ, নিরাপদ করে দেয়।**

---


### দরকারি কমান্ডগুলো (সবচেয়ে বেশি ব্যবহার হবে)

| কমান্ড                          | কাজ কী?                          |
| -------------------------------- | -------------------------------- |
| `npx prisma init`                | নতুন প্রজেক্টে Prisma শুরু করা   |
| `npx prisma migrate dev --name নাম` | Migration বানানো ও অ্যাপ্লাই করা |
| `npx prisma generate`            | Prisma Client আপডেট করা          |
| `npx prisma studio`              | ডাটাবেজ GUI-তে দেখা             |


---

## 🔧 Prisma Migration ও Client কীভাবে কাজ করে?

- **Migration**: তোমার schema.prisma ফাইলে পরিবর্তন করলে ডাটাবেজে সেই পরিবর্তন অ্যাপ্লাই করে।
- **Generate Client**: schema থেকে TypeScript কোড বানায় যাতে তুমি সহজে কোয়েরি লিখতে পারো।

সাধারণত `migrate dev` চালালে দুটোই অটো হয়ে যায়।

---

## 🧩 Prisma Client Setup – সহজ ভাষায় ব্যাখ্যা

### 🔹 কোড

```ts
import "dotenv/config";
import { PrismaPg } from "@prisma/adapter-pg";
import { PrismaClient } from "@prisma/client";  // বা তোমার জেনারেটেড পাথ

const connectionString = process.env.DATABASE_URL!;

const adapter = new PrismaPg({ connectionString });

const prisma = new PrismaClient({ adapter });

export { prisma };
```

---

## 🧠 লাইন বাই লাইন বুঝি

---

### 1️⃣ `dotenv` কেন ব্যবহার করা হয়েছে?

```ts
import "dotenv/config";
```

👉 `.env` ফাইলের ভেতরের ভ্যারিয়েবলগুলো (যেমন DATABASE_URL)
Node.js অ্যাপে ব্যবহার করার জন্য।

📌 মানে:
`.env` ফাইল → `process.env` এর মধ্যে লোড হয়।

---




### 2️⃣ Prisma PostgreSQL Adapter কেন?

```ts
import { PrismaPg } from "@prisma/adapter-pg";
```

👉 এটা Prisma-কে বলে দেয়:

> “আমি PostgreSQL database ব্যবহার করছি”

📌 নতুন Prisma adapter system-এ database connect করার জন্য এটা লাগে।

---




### 3️⃣ Prisma Client কী?

```ts
import { PrismaClient } from "@prisma/client";
```

👉 Prisma Client হলো database-এর সাথে কথা বলার টুল।

এটার মাধ্যমেই আমরা লিখি:

```ts
prisma.user.findMany()
```

---



### 4️⃣ DATABASE_URL নেওয়া

```ts
const connectionString = process.env.DATABASE_URL!;
```

👉 `.env` ফাইল থেকে database URL নেওয়া হচ্ছে।



### 5️⃣ Adapter তৈরি করা

```ts
const adapter = new PrismaPg({ connectionString });
```

👉 এখানে PostgreSQL adapter-কে database URL দেওয়া হচ্ছে।

মানে:

> Prisma এখন জানে কোন database-এ connect করতে হবে।

---



### 6️⃣ Prisma Client তৈরি করা

```ts
const prisma = new PrismaClient({ adapter });
```

👉 এখন Prisma Client তৈরি হলো, যেটা পুরো app-এ ব্যবহার হবে।

📌 এইটাই হলো main database instance।

---

### 7️⃣ Export করা

```ts
export { prisma };
```

👉 অন্য ফাইল থেকে ব্যবহার করার জন্য `prisma` export করা হচ্ছে।

---



## 🚀 কোথায় ব্যবহার করবো?

```ts
import { prisma } from "@/lib/prisma";

const users = await prisma.user.findMany();
```

👉 একই Prisma Client পুরো app-এ ব্যবহার করা হবে।

---

## 🧠 এক লাইনে পুরো কোড

**এই কোডটা PostgreSQL database-এর সাথে Prisma connect করে
এবং একটি shared Prisma Client তৈরি করে।**

---
> 

## 🔧 Prisma দিয়ে সহজ CRUD (Create, Read, Update, Delete)

### ➕ Create (নতুন ডাটা যোগ)

```ts
await prisma.user.create({
  data: { name: "Mahbuba", email: "mahbuba@example.com" },
});
```

### 📥 Read (ডাটা পড়া)

```ts
await prisma.user.findMany();  // সব ইউজার
await prisma.user.findUnique({ where: { id: 1 } });  // একটা
```

### ✏️ Update (আপডেট)

```ts
await prisma.user.update({
  where: { id: 1 },
  data: { name: "নতুন নাম" },
});
```

### ❌ Delete (ডিলিট)

```ts
await prisma.user.delete({ where: { id: 1 } });
```


---



# 📘 **Better Auth —  বাংলা গাইড**

## 🌟 Better Auth কী?

এটা একটা সহজ ও নিরাপদ **লগইন সিস্টেম** লাইব্রেরি।  
Prisma দিয়ে খুব তাড়াতাড়ি ইউজার সাইনআপ, লগইন, লগআউট বানানো যায়।

---
🚀 Step-by-Step Setup

# Step 1: Better Auth ইনস্টল করো

টার্মিনালে এই কমান্ড চালাও:

npm install better-auth


এতে Better Auth প্রজেক্টে যোগ হবে।



# Step 2: Secret Key তৈরি করো

Better Auth নিরাপত্তার জন্য একটি secret key ব্যবহার করে।

এই কমান্ড চালাও:

npx @better-auth/cli secret


👉 এটি একটি secret generate করবে।



# Step 3: .env ফাইলে Secret যোগ করো

.env ফাইলে নিচের ভ্যারিয়েবলগুলো অ্যাড করো:

BETTER_AUTH_SECRET=তোমার-সিক্রেট
BETTER_AUTH_URL=http://localhost:3000


📌 কেন দরকার?

BETTER_AUTH_SECRET → Authentication নিরাপদ রাখে

BETTER_AUTH_URL → তোমার অ্যাপের base URL





# Step 4: auth.ts ফাইল তৈরি করো

📁 Path: src/lib/auth.ts

import { betterAuth } from "better-auth";

import { prismaAdapter } from "better-auth/adapters/prisma";

import prisma from "./prisma";

export const auth = betterAuth({
 database: prismaAdapter(prisma, {
    provider: "postgresql",
  }),

 emailAndPassword: {
    enabled: true,
  },

});




📌 src/lib/auth.ts এখানে কী হচ্ছে?

Better Auth সেটআপ করা হচ্ছে

Prisma কে database হিসেবে ব্যবহার করা হচ্ছে

Email & Password login চালু করা হচ্ছে





# Step 5: Better Auth এর Database টেবিল যোগ করো

Better Auth নিজে কিছু টেবিল ব্যবহার করে (user, session ইত্যাদি)।


এই কমান্ডগুলো একে একে চালাও:

npx @better-auth/cli generate
npx prisma migrate dev --name auth
npx prisma generate



📌 এই কমান্ডগুলো যা করে:

Better Auth এর schema তৈরি করে

Database এ টেবিল যোগ করে

Prisma Client আপডেট করে

# Step 6: API Route তৈরি করো



## 🔑 দরকারি কমান্ড (Important Commands)

| কমান্ড | কী করে |
|------|-------|
| `npx @better-auth/cli generate` | Better Auth এর জন্য প্রয়োজনীয় database টেবিল তৈরি করে |
| `npx prisma migrate dev --name xyz` | Schema অনুযায়ী database আপডেট করে |
| `npx prisma studio` | Database দেখার জন্য GUI ওপেন করে |
-----------------------------------------------------------------------------
---------------------------------------------------------------------------
---

   # some web basics explain in bangla #

# 🍪 Cookie মানে কী? (সহজ ভাষায়)

**Cookie হলো ছোট ছোট ডেটা**, যা ব্রাউজার তোমার কম্পিউটারে সেভ করে রাখে।

এই ডেটা দিয়ে ব্রাউজার সার্ভারকে বারবার জানাতে পারে—
👉 “এই ইউজারটা কে”

---

## 🍪 Cookie কেন ব্যবহার করা হয়?

Cookie সাধারণত ব্যবহার হয়:

* ইউজার **লগইন করা আছে কিনা** মনে রাখার জন্য
* **Session ID** রাখার জন্য
* ইউজারের পছন্দ (Theme, Language) সেভ করার জন্য
* **Authentication token** (JWT / session cookie) পাঠানোর জন্য

---

## 🧠 বাস্তব উদাহরণ (Real Life Example)

ধরো তুমি একটি ওয়েবসাইটে লগইন করলে:

1️⃣ সার্ভার তোমাকে একটি **cookie** দেয়
2️⃣ ব্রাউজার সেই cookie সেভ করে রাখে
3️⃣ তুমি যখন আবার কোনো request পাঠাও
4️⃣ ব্রাউজার সেই cookie অটোমেটিকভাবে পাঠিয়ে দেয়
5️⃣ সার্ভার বুঝে যায় →
👉 “এই ইউজার তো আগেই লগইন করা!”

📌 **এজন্যই বারবার লগইন করতে হয় না।**

---

## ✨ এক লাইনে Cookie

**Cookie হলো ব্রাউজারে সেভ থাকা ছোট ডেটা, যা সার্ভারকে ইউজার চিনতে সাহায্য করে।**

---

# 🌐 CORS কী? (একদম সহজভাবে)

**CORS (Cross-Origin Resource Sharing)** হলো একটি
👉 **permission system**,
যা বলে দেয়—

> এক ওয়েবসাইট অন্য ওয়েবসাইটের ডেটা নিতে পারবে কিনা।

---

## 🔍 Origin মানে কী?

**Origin** হলো একটি ওয়েবসাইটের পরিচয় (identity)।

একটি origin তৈরি হয় ৩টি জিনিস দিয়ে:

1️⃣ **Protocol** → http / https
2️⃣ **Domain** → example.com
3️⃣ **Port** → 3000, 4000 ইত্যাদি

---

### 🧩 উদাহরণ:

```
http://localhost:3000
```

এই তিনটা একসাথে মিলে = **একটা origin**

👉 যদি protocol / domain / port এর যেকোনো একটা বদলায়
👉 তাহলে origin বদলে যায়।

---


---

## ❓ CORS কেন দরকার?

সিকিউরিটির জন্য ব্রাউজার **default ভাবে অনুমতি দেয় না**
একটা ওয়েবসাইট অন্য ওয়েবসাইটের ডেটা নিতে।

CORS বলে দেয়:

> “এই origin থেকে request আসলে allow করবো।”

---

## 🚀 সহজ উদাহরণ

* Frontend → `http://localhost:3000`
* Backend → `http://localhost:5000`

Frontend যখন backend-এ request পাঠায়,

👉 ব্রাউজার backend-কে জিজ্ঞেস করে:

> “এই origin কি allowed?”

👉 Backend যদি allow করে → request যাবে
👉 না করলে → browser error দেখাবে (CORS error)

---

## 🧩 Express-এ CORS কীভাবে কাজ করে?

Express-এ **CORS middleware** ব্যবহার করা হয়।

📌 Middleware মানে →
request আসার সময় মাঝখানে চেক করা

👉 Middleware ব্যবহার করতে হলে `app.use()` লিখতে হয়।

---

### ✅ সব origin allow (Development এর জন্য)

```js
const express = require('express');
const cors = require('cors');

const app = express();

app.use(cors());

app.listen(5000, () => {
  console.log('Server running on port 5000');
});
```

---

### 🎯 নির্দিষ্ট origin allow করতে চাইলে

```js
app.use(cors({
  origin: 'http://localhost:3000'
}));
```

---

### 🔒 Cookies ব্যবহার করলে (credentials true)

```js
app.use(cors({
  origin: 'http://localhost:3000',
  credentials: true
}));
```

---

## 🍪 credentials: true মানে কী? (খুব গুরুত্বপূর্ণ)

👉 `credentials: true` মানে—

**Browser কে বলা হচ্ছে:**

> “Request এর সাথে cookie পাঠাতে পারো”

📌 Better Auth এর ক্ষেত্রে:

* Token গুলো **cookie-এর ভিতরে সেভ থাকে**
* আমাদের আলাদা করে token handle করতে হয় না
* Login, logout সব সহজ হয়ে যায়

---

## 🧠 এক লাইনে CORS

**CORS হলো ব্রাউজারের security gatekeeper,
যে ঠিক করে কোন origin কোন origin-এর সাথে কথা বলতে পারবে।**

---



## 👩‍💻 লেখক

**Mahbuba**  
Full-Stack Developer  

শুভ কোডিং! 🚀
